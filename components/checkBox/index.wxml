<!-- components/checkBox/index.wxml -->
<view
 bind:tap="onChecked"
 class="container {{checked ? 'bg-checked' : 'bg-unchecked'}}"
>
	<l-icon
	 wx:if="{{checked?true:false}}"
	 name="check"
	 size="28"
	 l-class="sleeve"
	/>
</view>

